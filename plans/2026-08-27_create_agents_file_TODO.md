# TODO — Create Project AGENTS File

## Context
Add a concise project-level `AGENTS.md` so future agents understand the Google Apps Script dashboard architecture, its credential boundary, and the expected validation approach without altering application behavior.

## Tasks

- [x] 1. Create the project-level agent instructions.
       Files: `AGENTS.md`
       Done when: the file documents the Apps Script and HTML roles, protects Script Properties credentials, and specifies the existing Python validation command.

## Out of Scope

- Changing dashboard functionality or styling.
- Changing Jira credentials or Apps Script deployment settings.
- Adding dependencies, CI, or source control configuration.
